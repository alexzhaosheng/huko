// DEPRECATED: schema split into ./schema/infra.ts and ./schema/session.ts.
// This file is kept only because the Linux mount can't delete it from
// Windows-side ACLs. Import from the schema split files directly.
export {};
