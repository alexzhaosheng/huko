/**
 * server/cli/commands/list.ts
 *
 * @deprecated — superseded by `sessions.ts` (noun-first CLI shape).
 *
 * Kept as an empty stub because the Linux sandbox can't delete files
 * over Windows ACLs. Safe to remove this file on Windows:
 *   del server\cli\commands\list.ts
 *
 * No exports — nothing should import this.
 */
export {};
